# Google Analytics measurement ID. Leave unset so local/dev builds stay
# analytics-free. Set this in the GitHub Pages deploy workflow (or a local
# `.env` when you need a production-like preview).
# PUBLIC_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
